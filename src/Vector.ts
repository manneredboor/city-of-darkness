export interface Vector {
  x: number
  y: number
}

export const vec = (x: number, y: number) => ({ x, y })
// export class Vector {
//   x: number
//   y: number

//   constructor(x: number, y: number) {
//     this.x = x
//     this.y = y
//   }
// }
